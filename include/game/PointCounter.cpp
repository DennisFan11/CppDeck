

#include <vector>
# pragma once

int countPoint(
    std::vector<int> &publicNumber, // 公共区域牌号 (1-13)
    std::vector<int> &publicSuit, // 公共区域花色 (0-3)
    std::vector<int> &handNumber, // 手牌牌号 (1-13)
    std::vector<int> &handSuit // 手牌花色 (0-3)
    ){
    
    return 0;
}